"""Built-in workflow handler packages for the Antkeeper framework.

This package contains sub-packages that provide ready-to-use handler
collections grouped by LLM backend. Each sub-package exposes a pre-configured
``App`` instance whose handlers can be imported directly or merged into a
custom ``App``.

Available sub-packages:
    claude_code: SDLC handlers backed by the Claude Code CLI.
"""
